import { FC, ReactElement } from "react";

type InputPropsTypes = {
  placeholder: string;
  classes?: string;
  inputHandler: () => void;
};

export const Input: FC<InputPropsTypes> = (): ReactElement => <></>;
